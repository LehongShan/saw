server:
  port: 9003

spring:
  application:
    name: saw-auth-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LehongShan/SawConfigRepository
      label: master
      discovery:
        enabled: true
        service-id: saw-config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
